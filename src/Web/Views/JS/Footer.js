function goto(id) {
  window.location = `https://project-memory.glitch.me${id}`;
}


  // ServiceWorker is a progressive technology. Ignore unsupported browsers



function invalid(element) {
  $(element).addClass('is-invalid');
}
